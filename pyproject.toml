[project]
name = "locate-tenant"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.27.1",
    "pymysql>=1.2.0",
    "pyyaml>=6.0.3",
    "sqlalchemy>=2.0.49",
    "typer>=0.25.1",
    "typing-extensions>=4.15.0",
]

[project.scripts]
locate-tenant = "server:mcp.run"
